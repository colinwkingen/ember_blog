
<div class="col-md-6">
  <div id="article-tile">
      {{#link-to 'article' article.id}}
      <p><img src={{article.image}} alt={{article.title}}></p>
      <p>{{article.title}}</p>
      {{/link-to}}
  </div>
</div>
